# Type stub for the compatibility alias `xswap.reports` (INT-5612).
# The .py file replaces this module object with `xswap.core.reports` at runtime
# (sys.modules[__name__] = ...); pyright cannot follow that dynamic swap, so this
# stub tells it to type-check `from xswap.reports import X` as `from xswap.core.reports import X`.
from xswap.core.reports import *
