# Type stub for the compatibility alias `xswap.relocate` (INT-5612).
# The .py file replaces this module object with `xswap.providers.codex.relocate` at runtime
# (sys.modules[__name__] = ...); pyright cannot follow that dynamic swap, so this
# stub tells it to type-check `from xswap.relocate import X` as `from xswap.providers.codex.relocate import X`.
from xswap.providers.codex.relocate import *
