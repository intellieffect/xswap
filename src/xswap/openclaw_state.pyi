# Type stub for the compatibility alias `xswap.openclaw_state` (INT-5612).
# The .py file replaces this module object with `xswap.providers.codex.openclaw_state` at runtime
# (sys.modules[__name__] = ...); pyright cannot follow that dynamic swap, so this
# stub tells it to type-check `from xswap.openclaw_state import X` as `from xswap.providers.codex.openclaw_state import X`.
from xswap.providers.codex.openclaw_state import *
