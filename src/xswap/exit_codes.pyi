# Type stub for the compatibility alias `xswap.exit_codes` (INT-5612).
# The .py file replaces this module object with `xswap.core.exit_codes` at runtime
# (sys.modules[__name__] = ...); pyright cannot follow that dynamic swap, so this
# stub tells it to type-check `from xswap.exit_codes import X` as `from xswap.core.exit_codes import X`.
from xswap.core.exit_codes import *
