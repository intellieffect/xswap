# Type stub for the compatibility alias `xswap.usage_cache` (INT-5612).
# The .py file replaces this module object with `xswap.core.usage_cache` at runtime
# (sys.modules[__name__] = ...); pyright cannot follow that dynamic swap, so this
# stub tells it to type-check `from xswap.usage_cache import X` as `from xswap.core.usage_cache import X`.
from xswap.core.usage_cache import *
