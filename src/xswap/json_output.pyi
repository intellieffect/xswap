# Type stub for the compatibility alias `xswap.json_output` (INT-5612).
# The .py file replaces this module object with `xswap.core.json_output` at runtime
# (sys.modules[__name__] = ...); pyright cannot follow that dynamic swap, so this
# stub tells it to type-check `from xswap.json_output import X` as `from xswap.core.json_output import X`.
from xswap.core.json_output import *
