# Type stub for the compatibility alias `xswap.ranking` (INT-5612).
# The .py file replaces this module object with `xswap.core.ranking` at runtime
# (sys.modules[__name__] = ...); pyright cannot follow that dynamic swap, so this
# stub tells it to type-check `from xswap.ranking import X` as `from xswap.core.ranking import X`.
from xswap.core.ranking import *
